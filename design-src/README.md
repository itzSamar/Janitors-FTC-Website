# Design source (reference)

Unpacked from the friend’s Claude artifact HTML.

- `index.html` — full template (kept in sync with repo root `index.html` when possible)
- `app.logic.js` — page logic (`Component extends DCLogic`)
- `team-data.js` — `window.TEAM` build log / matches / auto route
- `design.css` — shared keyframes + tokens (also inlined in `index.html`)
- `assets/` — fonts + runtime copies

**Canonical live site files:** repo root `index.html` + `public/assets/`.
Edit those when changing the site; refresh these mirrors if useful.
